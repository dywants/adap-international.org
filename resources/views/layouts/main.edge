<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    @entryPointStyles('app')
    @entryPointScripts('app')
  </head>
  <body class="leading-normal tracking-normal flex flex-col min-h-screen ">
    @!component('components/front/navbar')

    <main class="relative grow overflow-hidden">
        @!section('body')
    </main>

    @!component('components/front/footer')
  </body>
</html>